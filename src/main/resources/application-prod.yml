spring:
  datasource:
    url: jdbc:mysql://db.host:3306/urlshortener?useSSL=false
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    show-sql: false