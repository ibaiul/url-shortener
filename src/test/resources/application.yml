server:
  port: 0
  http2:
    enabled: true

spring:
  datasource:
    url: jdbc:h2:mem:testdb;mode=MySQL
    username: root
    password: password
    driverClassName: org.h2.Driver
  h2:
    console:
      enabled: true
  jpa:
    database-platform: eus.ibai.urlshortener.config.H2DialectExtended
    show-sql: true
