spring:
  application:
    name: urlshortener
  datasource:
    driverClassName: com.mysql.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect

management:
  enabled: true

swagger:
  enabled: true
